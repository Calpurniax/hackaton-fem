import { FC } from "react";
import "../../styles/components/header.scss";

export const Header: FC = () => (
  <header className="c-header">
    <h1 className="c-header__title">Internet users evolution</h1>
  </header>
);
